{
  "name": "@ci/simulation",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "clean": "rimraf dist .turbo node_modules"
  },
  "devDependencies": {
    "@ci/config-typescript": "workspace:*",
    "@ci/config-vitest": "workspace:*",
    "@ci/commerce": "workspace:*",
    "@ci/runtime": "workspace:*"
  }
}
